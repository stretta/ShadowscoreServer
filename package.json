{
  "name": "shadowscore-server",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Shared ensemble score server for ShadowScore clients on Shadowbox hardware.",
  "scripts": {
    "start": "node src/server.mjs",
    "agent": "node src/registration-agent.mjs",
    "test": "node --test"
  },
  "engines": {
    "node": ">=20"
  }
}
